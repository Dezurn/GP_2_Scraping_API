# GP_2_Scraping_API
Групповой проект №2. DE: Scraping &amp; API
## Описание проекта
В рамках группового проекта командой была рассмотрена подробная статистика 
по вакансиям на таких сайтах, как:
- `rabota.ru`
- `superjob.ru`
## Участники проекта:
- Литвак Максим, ББИ234
- Большаков Владислав, ББИ234
- Шацкий Степан, ББИ234
- Коган Вячеслав, ББИ234
- Белов Кирилл, ББИ236 
- Исаков Артем, ББИ236
## Использованные библиотеки:
- `requests` — для отправки HTTP-запросов и получения данных по API и отдельным страницам вакансий;
- `selenium` — для загрузки страниц со списком вакансий и извлечения ссылок на вакансии;
- `BeautifulSoup4 (bs4)` — для извлечения структурированных данных из HTML-разметки;
- `pandas` — для формирования и сохранения итоговых датасетов, а также их анализа;
- `logging` — для логирования этапов сбора данных и возможных ошибок.
- `numpy` — для численных операций и обработки данных;
- `matplotlib` — для построения графиков;
- `seaborn` — для визуального анализа данных;
- `plotly` — для интерактивных визуализаций;
- `folium` — для построения интерактивных карт;
- `geopy` — для геокодирования адресов и получения координат.

## Проведенные работы

### 1. Superjob API
Для запросов к `API SuperJob` были использованы следующие запросы:

Запрос к списку городов для определения `id` нужного города (в нашем случае `Москва`, `Екатеринбург` и `Санкт-Петербург`);

Запрос к списку регионов для определения `id` нужного региона (в нашем случае это была `Московская область`);

Запрос к списку вакансий для определения `id` соответствующих параметрам вакансий (`регион`, `город`, `категория`);

Запрос к вакансии для получения и дальнейшего извлечения информации о вакансии;

Запрос к списку метро для конкретного города для получения названия `линии метро` и `цвета линии метро`;

Запрос к списку компаний для получения информации по конкретной компании для соответствующей вакансии.

### По результатам проведения данных запросов и извлечения информации из них был получен датасет superjob_all.csv, содержащий следующие признаки:

`id` — уникальный идентификатор вакансии в API SuperJob (число)

`Название вакансии` — наименование профессии, на которую компания ведет поиск (строка)

`Минимальная зарплата` — нижняя граница зарплаты (число)

`Максимальная зарплата` — верхняя граница зарплаты (число)

`Валюта` — денежная единица, в которой будет выдаваться зарплата (строка)

`Категория` — сфера, к которой относится вакансия (список строк)

`Детальные категории` — более узкая сфера, к которой относится вакансия (список строк)

`Город `— город, в котором размещена вакансия (строка)

`Метро` — станция метро, на которой размещена вакансия (список строк)

`Линия метро` — название линии метро, на которой размещена вакансия (список строк)

`Цвет линии метро` — цвет линии метро, на которой размещена вакансия (список строк)

`Адрес` — текстовый адрес места работы (строка)

`Id компании` — уникальный идентификатор компании, которая разместила вакансию, в API SuperJob (число)

`Индустрия` — сфера работы компании (список строк)

`Кол-во персонала в компании` — количество человек, работающих в компании (строка)

`Кол-во размещенных вакансий` — количество размещенных вакансий компанией (число)

`Название компании` — наименование компании, которая разместила вакансию (строка)

`Опыт` — требуемый опыт работы кандидата для вакансии (строка)

`График работы` — формат рабочего графика (строка)

`Образование` — требуемый уровень образования кандидата для вакансии (строка)

`Широта` — географическая широта расположения работы (число)

`Долгота` — географическая долгота расположения работы (число)

`Ссылка` — прямая ссылка на вакансию (строка)

### Также был получен датасет rabota_ru.csv, содержащий следующие признаки:

`Название вакансии` — наименование профессии, на которую компания ведет поиск (строка)

`Зарплата` — диапазон/минимальное значение/максимальное значение/среднее значение денежных средств, которое будет получать человек на данной вакансии (строка)

`Требуемые навыки` — требуемые навыки кандидата для вакансии (список строк)

`Теги` — дополнительные характеристики вакансии, указанные на сайте (список строк)

`Компания` — наименование компании, которая выложила вакансию (строка)

`Метро` — станции/станция метро, на которой расположена вакансия (список строк)

`Адрес` — адрес, на котором расположена вакансия (строка)

`Город` — город, в котором расположена вакансия (строка)

`Сфера` — сфера, к которой относится вакансия (строка)

`Общее название профессии` — более широкое название вакансии (строка)

`Опыт работы` — требуемый опыт работы кандидата для вакансии (строка)

`Образование` — требуемый уровень образования кандидата для вакансии (строка)

`Ссылка` — прямая ссылка на вакансию (строка)

## После сбора и предобработки данных был проведён анализ вакансий. 
### В рамках EDA было исследованно:
- распределение вакансий по городам;
- распределение вакансий по работодателям;
- анализ уровня заработных плат, включая минимальные, максимальные и средние значения;
- выявление выбросов;
- анализ зависимости зарплаты от опыта работы;
- анализ зависимости зарплаты от уровня образования;
- анализ наиболее часто встречающихся профессий;
- анализ сфер деятельности и категорий вакансий;
- анализ требуемых навыков и дополнительных тегов вакансий;
- анализ наиболее активных работодателей по количеству размещённых вакансий;
- сравнение работодателей по медианной заработной плате;
- исследование адресов и географических координат вакансий;
- построение карт распределения вакансий;
- анализ распределения вакансий по станциям и линиям метро.

## Выводы

1. Анализ показал, что рынок вакансий в нашей выборке во многом держится на нескольких крупных работодателях. Именно они публикуют больше всего вакансий и формируют основную часть спроса. Получается, что рынок несбалансирован: большая доля вакансий принадлежит конкретным крупным игрокам.

2. Компании, которые публикуют больше всего вакансий, не всегда предлагают самые высокие зарплаты. Чаще всего это работодатели массового найма, которым постоянно нужен персонал. При этом компании с более высокой медианной зарплатой обычно размещают меньше вакансий, то есть ориентируются не на массовый подбор, а на поиск более подходящих и квалифицированных специалистов.

3. Размер компании сам по себе не гарантирует высокую зарплату. По данным видно, что некоторые крупные работодатели предлагают довольно умеренный уровень оплаты, тогда как часть менее крупных компаний платит заметно больше. Это можно объяснить тем, что небольшим и средним компаниям часто приходится конкурировать за сотрудников именно за счёт зарплаты.

4. Вакансии распределены по городам и локациям неравномерно. Наибольшая активность ожидаемо наблюдается в крупных городах, а внутри самих городов вакансии чаще сосредоточены в районах с хорошей транспортной доступностью. 

